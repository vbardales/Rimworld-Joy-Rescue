# Pickle runs

One line per run, newest last. A run's evidence stays on disk and out of git (see
`Tests/Pickle/README.md`, "Evidence to keep"); what is committed is this history, never folders.

| Date | Pass | Result | Evidence kept |
| --- | --- | --- | --- |
| 2026-09-22 23:14 | shared job, chess then ur (`03` then `05`), PID 22208 | infrastructure-error, code 137, 0 scenarios played, reader launch never ran | the queue stdout log, `Tests/Pickle/Evidence/F14-chess-then-ur-queue.stdout.log` |
| 2026-09-24 about 12:55 | shared job, four launches on one ticket (`03`, `05`, `04`, `05`), PID 52588 | launch 1 (`03`) passed, 1 of 1, game exit 137 after a complete report, kept. Launch 2 (`05`) failed, 0 of 1: `JoyRescueWitness_PlayChess kind is Gaming_Cerebral, expected Artistic`. The chain stopped, `04` and the second `05` did not play. Cause: a test defect, the fixture assigned `Artistic`, which is not a Core recreation kind, and the mod logged that it skipped it. Fixed in `SharedJobSteps.cs` (now `Social`, checked to exist by the writer). | deleted once the passing rerun below replaced it |
| 2026-09-24, Player.log 16:40 (WSL clock, UTC) | F14 chess then ur, two launches (`03` then `05`), request `20260924-163956-958-a63d` through the dispatcher's worker | passed both: `03` 1 of 1 (the writer) and `05` 1 of 1 (the reader after a real restart), `exitReason: passed` each, worker exit 0, one attempt. The reader found chess on `Social` and Ur on `Gaming_Dexterity`, with independent jobs crediting their kinds, and the log holds no skipped reassignment. The launcher also left a third folder, an exact copy of the second, deleted. | `Tests/Pickle/Evidence/F14-20260924-chess-then-ur/seq1` and `seq2`: `summary.json`, `summary.md`, `junit.xml`, `Player.log` |
| 2026-09-25 03:20 | final pass, minimal English, no filter, request `20260924-205336-502-2da8` | passed: 10 discovered, 4 played and passed (all of `01`), 6 skipped as designed (`02` to `05`, their mods are not staged), `exitReason: passed`, worker exit 0, one attempt. Both `@review` captures opened: the real Joy Rescue settings window in English, the same one from Mod options and from the MainButtons shortcut, counters and controls fit, no raw key or clipped text. | `Tests/Pickle/Evidence/final-english`: `summary.json`, `summary.md`, `junit.xml`, the two captures as jpeg; report, ndjson and `Player.log` deleted |
| 2026-09-25 | final pass, minimal French, no filter, request `20260924-205337-355-3d24` | passed: 10 discovered, 4 played and passed (all of `01`), 6 skipped as designed, `exitReason: passed`, worker exit 0, one attempt. Both `@review` captures opened: the settings window fully in French, from Mod options and from the shortcut, with the counters, the toggles, the eight buttons, the two type groups and the activity names all translated, no raw key, no English fallback, nothing clipped. | `Tests/Pickle/Evidence/final-french`: `summary.json`, `summary.md`, `junit.xml`, the two captures as jpeg; report, ndjson and `Player.log` deleted |
| 2026-09-25 | final pass, with RIMMSQOL, no filter, request `20260924-205338-490-f62b` | passed: 10 discovered, 7 played and passed (all of `01` and the three of `02`), 3 skipped as designed (`03` to `05`, the witness mod is not staged), `exitReason: passed`, worker exit 0, one attempt. Four `@review` captures opened: the settings window from Mod options and from the default shortcut, with no Joy Rescue button in the bar; the RIMMSQOL "Select Main Button" list, which lists Joy Rescue; and the same window opened from the revealed Joy Rescue button, now in the bar. One tooltip shows the def name in parentheses, `(Play_GameOfUr)`. RIMMSQOL is Workshop item 1084452457 as staged, its version number is not printed in the log. | `Tests/Pickle/Evidence/final-rimmsqol`: `summary.json`, `summary.md`, `junit.xml`, the four captures as jpeg; report, ndjson and `Player.log` deleted |
| 2026-09-25 11:06 | final pass, F14 ur then chess, two launches (`04` then `05`), request `20260924-205339-283-e105` | passed both: `04` 1 of 1 (the writer, reverse insertion order) and `05` 1 of 1 (the reader after a real restart), `exitReason: passed` each, worker exit 0, one attempt. The reader found the assigned kinds and independent jobs again, and the log holds no skipped reassignment; its only error is the known one of the content-less test companion. Ran on the new DLL (`9737F8F9...`). No third folder this time. | `Tests/Pickle/Evidence/final-f14-ur-then-chess/seq1` and `seq2`: `summary.json`, `summary.md`, `junit.xml`; reports, ndjson and `Player.log` deleted |
